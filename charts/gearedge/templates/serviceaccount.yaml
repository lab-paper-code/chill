{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "gearedge.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gearedge.labels" . | nindent 4 }}
{{- end }}

{{- if .Values.crds.enabled }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
    helm.sh/resource-policy: keep
  name: deviceprofiles.edge.dacs.io
spec:
  group: edge.dacs.io
  names:
    kind: DeviceProfile
    listKind: DeviceProfileList
    plural: deviceprofiles
    singular: deviceprofile
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeviceProfile is the Schema for the deviceprofiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeviceProfileSpec defines the desired state of DeviceProfile
            properties:
              foo:
                description: Foo is an example field of DeviceProfile. Edit deviceprofile_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: DeviceProfileStatus defines the observed state of DeviceProfile
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chill.operatorName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: operator
    {{- include "chill.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.operator.replicaCount }}
  selector:
    matchLabels:
      {{- include "chill.operatorSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: operator
      labels:
        {{- include "chill.operatorSelectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "chill.operatorServiceAccountName" . }}
      securityContext:
        {{- toYaml .Values.operator.podSecurityContext | nindent 8 }}
      {{- with .Values.operator.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.operator.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.operator.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: operator
          image: "{{ include "chill.operatorImage" . }}"
          imagePullPolicy: {{ .Values.operator.image.pullPolicy }}
          command:
            - /operator
          args:
            - --metrics-bind-address={{ .Values.operator.metricsBindAddress }}
            - --health-probe-bind-address={{ .Values.operator.healthProbeBindAddress }}
            - --system-name={{ include "chill.systemName" . }}
            - --operator-namespace={{ .Release.Namespace }}
            - --operator-deployment-name={{ include "chill.operatorName" . }}
            - --system-refresh-interval={{ .Values.system.refreshInterval }}
            - --node-discovery-config-key={{ .Values.nodeDiscovery.configMapKey }}
            - --node-discovery-reconcile-interval={{ .Values.operator.nodeDiscoveryReconcileInterval }}
            {{- if .Values.operator.leaderElection }}
            - --leader-elect
            - --leader-election-id={{ .Values.operator.leaderElectionID }}
            {{- end }}
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          securityContext:
            {{- toYaml .Values.operator.securityContext | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.operator.resources | nindent 12 }}
      terminationGracePeriodSeconds: 10

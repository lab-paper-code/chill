---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: deviceclasses.edge.dacs.io
spec:
  group: edge.dacs.io
  names:
    kind: DeviceClass
    listKind: DeviceClassList
    plural: deviceclasses
    singular: deviceclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeviceClass is the Schema for the deviceclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeviceClassSpec defines the desired state of DeviceClass
            properties:
              accelerator:
                description: Accelerator names the accelerator family exposed by this
                  class, or "none".
                type: string
              architecture:
                description: Architecture is the CPU architecture exposed by Kubernetes
                  for this class.
                minLength: 1
                type: string
              memory:
                anyOf:
                - type: integer
                - type: string
                description: MemoryBytes is the total unified or system memory available
                  on this class.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  NodeSelector selects nodes that belong to this device class.
                  Discovery starts as explicit label matching; automatic node feature
                  discovery is intentionally outside this resource's first schema.
                minProperties: 1
                type: object
              powerModes:
                description: PowerModes lists the power modes supported by this class.
                items:
                  description: PowerMode describes one runtime-selectable power mode
                    for a DeviceClass.
                  properties:
                    name:
                      description: Name is the human-readable platform power mode
                        name.
                      minLength: 1
                      type: string
                    nvpId:
                      description: NvpID is the nvpmodel -m identifier for Jetson
                        devices.
                      minimum: 0
                      type: integer
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - architecture
            - memory
            - nodeSelector
            - powerModes
            type: object
          status:
            description: DeviceClassStatus defines the observed state of DeviceClass
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
